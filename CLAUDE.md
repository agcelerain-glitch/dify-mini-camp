# Dify mini Camp 開発要件定義＆プロジェクトドキュメント

## 1. プロジェクト概要
- **プロジェクト名:** Dify mini Camp
- **コンセプト:** 「Difyの学習をDifyを使って行う」斬新なAIメンター付き学習プラットフォーム
- **背景:** 開発者自身がDifyを完全に使いこなせていないという課題から出発。自分自身が学習しながら、同じ悩みを持つユーザーをターゲットにした実践的な教材アプリを開発する。Difyがとりあえず動くを実感してもらうために初学者は1h程度で終わることを想定。
- **目的:** ユーザーがDifyの基礎から高度なオーケストレーション機能まで、段階的にハンズオンで学習し、最終的に自力で複雑なチャットボットやワークフローを構築できるようになること。
- **ターゲット層:** Difyを使いこなしたい初学者〜中級者。PCでのサクサクとした操作感を重視。

- 内容
  Dify AIメンターから「LLMブロックの次は何ブロックを繋ぎましたか？」や「Difyの画面右上の『公開する』ボタンを押しましたか？」といった確認の質問（一問一答）を投げ、それに正しく答えたらPhase 1クリア（DBに記録）とする
  AIメンターの「キャラクター（ペルソナ）」設定：Difyでチャットボットを作る際、システムプロンプトにキャラクターを持たせると学習が楽しくなります。
    「優しい先輩エンジニア」「ちょっと厳しいスパルタ教師」「一緒に悩んでくれる同僚」など、どのようなトーンでユーザーと壁打ちさせたいですか？
  PCでの「サクサク感」と「モダンな見た目」を最速で作るために、Next.jsと相性の良い Tailwind CSS + shadcn/ui の組み合わせ
  SupabaseのシンプルなDB設計（初期段階）：モック段階では複雑なテーブルは不要です。以下のようなシンプルな構成を想定しておくと良いです。
    users テーブル: ID, ユーザー名, メールアドレス（Google連携）
    progress テーブル: ユーザーID, 現在のフェーズ(1~5), 各フェーズのクリア日時

- フェーズ,学習テーマ,Difyでの実践内容,出題・解答形式
    Phase 1初級,LLMの基本とGUI操作,開始 -> LLM -> 回答の3ブロック構成。Difyの画面構成と基本用語の理解。,一問一答形式AIからのシンプルな質問に対し、選択肢や短答で答える。
    Phase 2初中級,変数とプロンプトエンジニアリング,変数の概念（ユーザー入力の受け取り）。システムプロンプトの記述方法。,穴埋め・短い記述式「この変数を使ってどうプロンプトを書くか」をテキストで答える。
    Phase 3中級,条件分岐とロジック,質問分類器（Classifier）、If/Elseブロックの理解。意図に応じたルート分岐。,シナリオ提示式「クレームと質問を分けるには？」といったお題に対し、必要なブロックを答える。
    Phase 4中上級,ナレッジ機能と環境変数,RAG（検索拡張生成）の仕組み。環境変数の安全な格納とツールの基礎。,画像アップロード/長文記述自分が作ったDifyの設定画面をスクショしてAIに判定してもらう。
    Phase 5上級,オーケストレートと堅牢性,複数ブロックの連携、複雑なワークフロー設計。プロンプトインジェクション等のセキュリティ対策。,実践壁打ち・トラブルシューティングエラーが起きるワークフローの画像を見て原因を指摘する。


## 2. システム要件・技術スタック
- **フロントエンド:** Next.js (React)
- **バックエンド / データベース / 認証:** Supabase
  - Googleログインによるユーザー認証
  - 学習進捗（各フェーズのクリア状況）の記録
- **ホスティング:** Vercel
- **AIエンジン (バックエンドロジック):** Dify API
  - フロントサイトに独自のチャットUIを構築し、バックグラウンドでDifyのワークフローと通信。
- **ソースコード管理:** GitHub
- **セキュリティ要件:** 
  - VercelおよびSupabaseの環境変数(`.env`)を活用。
  - DifyのAPIキーはフロントエンドに絶対に露出させず、Next.jsのサーバーサイド（API Routes / Server Actions）を経由してDifyと通信するセキュアな設計とする。

## 3. 学習カリキュラム（5フェーズ構成）

| フェーズ | 学習テーマ | 目的とゴール | Difyでの実践内容 | 出題・解答形式 | 推奨学習時間 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Phase 1<br>初級** | LLMの基本とGUI操作 | **目的:** Difyの画面に慣れ、とにかく動くものを作る。<br>**ゴール:** 開始→LLM→回答の3ブロック構成のアプリを自力で作成できる。 | Difyの基本UIの把握、ノードの繋ぎ方、基本的なテキスト生成アプリの作成。 | **一問一答形式**<br>AIメンターからのシンプルな質問に対し、選択肢や短答で答える。 | 45分〜1時間 |
| **Phase 2<br>初中級** | 変数とプロンプトエンジニアリング | **目的:** ユーザー入力を動的に扱う仕組みを理解する。<br>**ゴール:** 変数を用いたシステムプロンプトを設計し、狙った出力を得られる。 | 変数の概念理解（ユーザー入力の受け取り）、システムプロンプトの記述方法。 | **穴埋め・短い記述式**<br>「この変数を使ってどうプロンプトを書くか」をテキストで答える。 | 1.5時間 |
| **Phase 3<br>中級** | 条件分岐とロジック | **目的:** フローに柔軟性を持たせる。<br>**ゴール:** ユーザーの意図を汲み取り、適切な処理ルートへ分岐させるワークフローを構築できる。 | 質問分類器（Classifier）、If/Elseブロックの理解、意図に応じたルート分岐。 | **シナリオ提示式**<br>「クレームと質問を分けるには？」といったお題に対し、必要なブロックや条件を答える。オーケストレートの各種ボタンの説明なども加え、時短知識や活用機能の推奨を行う。 | 2時間 |
| **Phase 4<br>中上級** | ナレッジ機能と環境変数 | **目的:** 外部知識の取り込みとセキュアな設定を学ぶ。<br>**ゴール:** RAGを実装し、APIキー等の環境変数を正しい場所に格納できる。 | RAG（検索拡張生成）の仕組み、環境変数の安全な格納、ツールの基礎。 | **画像アップロード/長文記述**<br>自分が作ったDifyの設定画面をスクショしてAIに判定してもらう。 | 2.5時間 |
| **Phase 5<br>上級** | オーケストレートと堅牢性 | **目的:** 実務レベルの複雑なアプリ構築。<br>**ゴール:** 複数ブロックを連携させ、プロンプトインジェクション等のリスクに対応した堅牢なアプリを作れる。 | 複雑なワークフロー設計、プロンプトインジェクション等のセキュリティ対策。 | **実践壁打ち・トラブルシューティング**<br>エラーが起きるワークフローの画像を見て原因を指摘・修正する。 | 3時間 |

## 4. 開発ロードマップ（To-Do）
1. **[現在] モックアップ作成（Phase 1フォーカス）**
   - Next.js環境構築
   - 静的な画面レイアウト（チャットUI、フェーズ進行UI）の作成
2. **認証・データベース連携**
   - Supabaseプロジェクトのセットアップ
   - Googleログイン機能の実装
   - ユーザー情報およびPhase進捗を保存するDB設計
3. **Dify API連携**
   - Next.jsサーバーサイドからDify APIを叩く処理の実装
   - Dify側での「AIメンター」ワークフローの初期構築（一問一答形式の処理）
4. **学習コンテンツの拡充**
   - Phase 2〜5のUIおよびDify側ワークフローの構築
5. **テスト・デプロイ**
   - Vercelへのデプロイ
   - テストユーザーによるα版（デモ版）テスト運用

## 6. コンテンツ追加ルール（必須チェック）

### ID体系の原則
`front/lib/phases-data.ts` の `page.id` / `level.id` は **Supabase の `clearedPages` 配列に保存される安定識別子（DB主キー相当）** です。
**表示番号（P1, P2...）は配列インデックス+1 で決まります。IDとは別物です。**

### コンテンツ追加時の必須作業

#### ページを追加するとき
1. **新IDの決定**：対象レベル内の「使用済みID最大値 + 1」を採用する。ファイル冒頭の採番テーブルを参照し、追加後は必ずテーブルを更新する。
2. **既存IDは不変**：既存ページのIDは配列内での位置が変わっても絶対に変更しない。
3. **欠番は永久欠番**：削除したIDは再利用しない。
4. **挿入位置は自由**：途中挿入OK。表示順は配列の並び順で決まる。

#### レベルを追加するとき
1. 対象フェーズ内の「使用済みレベルID最大値 + 1」を新IDとして使う。
2. 既存レベルのIDは変更しない。

### 堅牢化ロジック（変更禁止）
以下のロジックはID非連続・途中挿入に対応するために実装済みです。変更・削除禁止。

| ロジック | 実装箇所 | 効果 |
|---------|---------|------|
| クリア済みページ → 常にアンロック | `ProgressContext.tsx` `isPageUnlocked` | コンテンツ追加後も既存ユーザーの進捗が壊れない |
| クリア済みレベル → 常にアンロック | `ProgressContext.tsx` `isLevelUnlocked` | 新レベル挿入後も前後のアクセスが維持される |
| 進捗計算 = ページIDの照合方式 | `ProgressContext.tsx` `getLevelProgress` / `getPhaseProgress` | 配列長に依存しない正確な計算 |
| 表示番号 = 配列インデックス+1 | `PhaseContent.tsx` サイドバー・タブ・パンくず・ヘッダー | IDと表示番号の乖離を吸収 |

### コミット前チェックリスト
- [ ] 新ページ/レベルのIDは採番テーブルの「次ID」を使った
- [ ] `phases-data.ts` 冒頭の採番テーブルを更新した
- [ ] 既存ページ/レベルのIDを変更していない
- [ ] `isPageUnlocked` / `isLevelUnlocked` に「クリア済み→常にアンロック」が残っている
- [ ] 進捗計算がID照合方式のまま（`.length` や連番仮定になっていない）

---

## 5. 将来の発展アイデア（将来設計）
モック完成・ローンチ後に実装を検討する拡張機能：
- **クライアント・ロールプレイ機能:** AIが「要件のフワッとしたクライアント」として振る舞い、ユーザーが適切なDifyの構成をヒアリング・提案する実践的モード（Phase 3以降）。
- **Dify DSL (.yml) エクスポート報酬:** 各フェーズをクリアすると、手本となるDifyアプリのテンプレートファイルがダウンロードでき、ユーザーが自身の環境ですぐに試せる仕組み。
- **バグ探し（トラブルシューティング）モード:** ワザとエラーが起きるワークフローの画像を提示し、どこが間違っているかを指摘させる上級者向けコンテンツ。
- **Dify APIによる高度な「自動採点・添削」:** ユーザーが入力した長文やアップロードした画像をAIが解析し、単なる正誤判定だけでなく「ここはこうするともっとスマートな構成になる」といったコードレビュー的なフィードバックを行う。
